# Dependencies (라이브러리 폴더 - 용량이 커서 올리면 안 됨)
node_modules/

# Production (빌드 결과물)
dist/
dist-ssr/

# Environment Variables (보안 - 절대 올리면 안 됨!)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*.local

# macOS (맥 시스템 파일)
.DS_Store

# Logs (에러 로그)
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor directories (에디터 설정 파일)
.idea
.vscode/
*.suo
*.ntvs
*.njsproj
*.sln
*.sw?